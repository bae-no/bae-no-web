query myProfileQuery {
  myProfile {
    nickname
  }
  myEndDealCount
}

query userAddressQuery {
  addresses {
    alias
    coordinate {
      latitude
      longitude
    }
    detail
    key
    path
    system
    type
  }
}
